import React from "react";
import { cn } from "@/lib/utils";
const Feed = ({ className }: { className?: string }) => {
  return <div className={cn("", className)}>Feed</div>;
};

export default Feed;
